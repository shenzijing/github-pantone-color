<%- include('layout', { body: `
<div class="max-w-4xl mx-auto">
    <a href="/blog" class="text-blue-500 hover:text-blue-600 mb-4 inline-block">&larr; <%= t('backToBlog') %></a>
    <h1 class="text-4xl font-bold mb-6"><%= post.title %></h1>
    <p class="text-gray-500 mb-8"><%= post.date %></p>
    <div class="prose lg:prose-xl"><%- post.content %></div>
</div>
` }) %>